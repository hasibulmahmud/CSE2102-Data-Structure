#include<bits/stdc++.h>
using namespace std;

int main(){
    string str ="562+*24/-";
    int val=0;
    int n =str.size();
    stack<int> st;
    for(int i=0;i<n;i++){
        if(str[i]>='0' && str[i]<='9'){
            st.push(str[i]-'0');
        }else{
        int a=st.top();
        st.pop();
        int b=st.top();
        st.pop();
        if(str[i]=='+'){
            val = b+a;
        }else if(str[i]=='-'){
        val = b-a;
        }else if(str[i]=='*'){
        val = a*b;
        }else{
        val=b/a;
        }
        st.push(val);
        }
    }
    cout << st.top() <<endl;
}
